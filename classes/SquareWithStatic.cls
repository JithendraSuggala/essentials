public class SquareWithStatic {
public static void perimeter(Decimal l){
        decimal perimeter=4*l;
        System.debug('Perimeter is '+perimeter);
    }
    public static void area(Decimal l){

        decimal area=l*l;
        System.debug('area is '+area);
    }
}